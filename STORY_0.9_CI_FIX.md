# Story 0.9 — CI pipeline — Исправление ошибок CI

## Проблема

Все 3 CI jobs провалились:
- ❌ CI / Build (pull_request): Failing
- ❌ CI / Lint (pull_request): Failing  
- ❌ CI / TypeCheck (pull_request): Failing

## Причина

В репозитории нет `pnpm-lock.yaml`, но CI пытается использовать `pnpm install`, что приводит к ошибке.

## Решение

Обновлён `.github/workflows/ci.yml`:
- Добавлена проверка наличия `pnpm-lock.yaml`
- Если файл есть - используется `pnpm install --frozen-lockfile`
- Если файла нет - используется `npm install`

## Исправление применено

✅ Изменения закоммичены в ветку `test/ci-pipeline`
✅ Ветка запушена в GitHub
✅ CI запустится автоматически с исправлениями

## Следующие шаги

1. **Дождаться завершения нового CI run** (2-5 минут)
2. **Проверить статус jobs** в PR
3. **Убедиться что все 3 jobs прошли успешно:**
   - ✅ Lint
   - ✅ TypeCheck
   - ✅ Build

**После исправления все jobs должны пройти успешно!** 🎉
