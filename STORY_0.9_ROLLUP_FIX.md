# Story 0.9 — Исправление ошибки rollup native modules ✅

## Проблема

Build job падает с ошибкой:
```
Error: Cannot find module @rollup/rollup-linux-x64-gnu
npm has a bug related to optional dependencies (https://github.com/npm/cli/issues/4828)
```

Причина: npm workspaces не всегда правильно устанавливают optional dependencies для нативных модулей (rollup требует нативные модули для Linux).

## Решение

✅ Добавлен шаг для переустановки rollup в `apps/webapp`:
- Удаляются `node_modules/@rollup`
- Переустанавливается `rollup` с флагом `--force`

Это обходит баг npm с optional dependencies в workspaces.

## Следующие шаги

1. **Обновить страницу PR** (Ctrl+F5)
2. **Дождаться завершения нового CI run** (2-3 минуты)
3. **Проверить статус Build job:**
   - ✅ Должен пройти успешно

**Исправление применено!** 🎉

Если это не поможет, можно попробовать:
- Использовать `npm install --force` в корне
- Или установить зависимости в каждом workspace отдельно
