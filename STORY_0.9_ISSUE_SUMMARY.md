# Story 0.9 — CI pipeline — Проблема и решение

## Текущая ситуация

Все 3 CI jobs проваливаются. Основная проблема: **файлы проекта не закоммичены в git репозиторий**.

## Что нужно сделать

### Вариант 1: Добавить файлы вручную (рекомендуется)

1. **Убедитесь что все файлы проекта есть локально:**
   ```bash
   ls -la package.json turbo.json tsconfig.json
   ls -la apps/ packages/ infra/
   ```

2. **Добавьте все файлы в git:**
   ```bash
   git checkout test/ci-pipeline
   git add package.json turbo.json tsconfig.json pnpm-workspace.yaml
   git add apps/ packages/ infra/ scripts/
   git add .gitignore .eslintrc.json .prettierrc README.md
   git commit -m "chore: add project files for CI"
   git push origin test/ci-pipeline
   ```

3. **Проверьте что файлы добавлены:**
   ```bash
   git ls-files | grep -E "^package.json$|^turbo.json$"
   ```

### Вариант 2: Проверить логи CI

Откройте PR в браузере и нажмите на каждый провалившийся job, чтобы увидеть логи ошибок. Это поможет понять точную причину.

## Что уже исправлено

✅ CI workflow использует `npm` вместо `pnpm`
✅ CI workflow настроен правильно
✅ PR создан

## Что осталось

⏳ Добавить файлы проекта в git репозиторий

**После добавления файлов CI должен заработать!** 🎉
