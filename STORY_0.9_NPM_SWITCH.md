# Story 0.9 — Переход с pnpm на npm ✅

## Решение

Пользователь прав - проще использовать npm, так как он уже установлен в GitHub Actions по умолчанию.

## Изменения

✅ **package.json:**
- Удалён `"packageManager": "pnpm@8.15.0"`
- Удалён `"pnpm": ">=8.0.0"` из `engines`
- Добавлен `"workspaces"` для npm workspaces

✅ **pnpm-workspace.yaml:**
- Удалён (больше не нужен)

✅ **.github/workflows/ci.yml:**
- Удалён шаг "Setup pnpm"
- Изменён кэш на `npm`
- Все команды используют `npm`:
  - `npm install`
  - `npm run lint`
  - `npm run typecheck`
  - `npm run build`

✅ **README.md:**
- Обновлены все упоминания `pnpm` на `npm`

## Преимущества

- ✅ npm уже установлен в GitHub Actions (не нужен дополнительный шаг)
- ✅ Меньше зависимостей
- ✅ Проще настройка CI
- ✅ Turbo работает с npm workspaces так же хорошо

## Следующие шаги

1. **Дождаться завершения нового CI run** (2-3 минуты)
2. **Обновить страницу PR** (Ctrl+F5)
3. **Проверить статус всех 3 jobs:**
   - ✅ Lint
   - ✅ TypeCheck
   - ✅ Build

**Теперь CI должен работать с npm!** 🎉
