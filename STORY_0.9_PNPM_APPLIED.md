# Story 0.9 — CI pipeline — pnpm установлен в CI ✅

## Проблема

Build job провалился с ошибкой:
```
unable to find package manager binary: cannot find binary path
```

Turbo требует pnpm (указан в `package.json` как `"packageManager": "pnpm@8.15.0"`), но в CI использовался npm.

## Решение

✅ Обновлён `.github/workflows/ci.yml`:
- Добавлен шаг "Setup pnpm" с версией 8.15.0 во все jobs
- Изменён кэш на `pnpm`
- Все команды изменены на `pnpm`:
  - `pnpm install`
  - `pnpm lint`
  - `pnpm typecheck`
  - `pnpm build`

✅ Изменения закоммичены и запушены

## Следующие шаги

1. **Дождаться завершения нового CI run** (2-3 минуты)
2. **Обновить страницу PR** в браузере (Ctrl+F5)
3. **Проверить статус CI jobs:**
   - Должны быть зелёные галочки ✅
   - Все 3 jobs должны пройти: Lint, TypeCheck, Build

**Теперь CI должен работать!** 🎉

После установки pnpm в CI, turbo сможет найти package manager и выполнить все команды успешно.

Обновите PR через 2-3 минуты и проверьте статус CI. Все jobs должны пройти успешно.
