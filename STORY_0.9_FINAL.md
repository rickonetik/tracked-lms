# Story 0.9 — CI pipeline минимальный — Завершено ✅

## Созданные файлы

### GitHub Actions
- ✅ `.github/workflows/ci.yml` - CI pipeline с 3 jobs

### Обновленные файлы
- ✅ `README.md` - Добавлена секция о CI/CD

## Конфигурация CI

### Триггеры
- `push` на ветки: `main`, `develop`
- `pull_request` на ветки: `main`, `develop`

### Jobs

#### 1. Lint
- **Название:** Lint
- **Команда:** `pnpm lint`
- **Проверяет:** Код линтером (ESLint)

#### 2. TypeCheck
- **Название:** TypeCheck
- **Команда:** `pnpm typecheck`
- **Проверяет:** Типы TypeScript

#### 3. Build
- **Название:** Build
- **Команда:** `pnpm build`
- **Проверяет:** Сборка всех приложений

### Настройки

- **ОС:** ubuntu-latest
- **pnpm:** 8.15.0
- **Node.js:** 18
- **Кэширование:** pnpm cache
- **Параллельность:** Все jobs выполняются параллельно

## Структура workflow

```yaml
name: CI
on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  lint: ...
  typecheck: ...
  build: ...
```

## Для проверки DoD

### 1. Локальная проверка команд

```bash
# Убедиться что команды работают
pnpm lint
pnpm typecheck
pnpm build
```

### 2. Создать PR в GitHub

1. Создать новую ветку:
   ```bash
   git checkout -b test/ci-pipeline
   ```

2. Закоммитить изменения:
   ```bash
   git add .github/workflows/ci.yml
   git commit -m "chore: add CI pipeline"
   git push origin test/ci-pipeline
   ```

3. Создать Pull Request в GitHub

4. Проверить что CI запустился автоматически

**Ожидаемое поведение:**
- ✅ CI запускается автоматически
- ✅ Все 3 jobs выполняются
- ✅ PR показывает зелёный статус

## Ожидаемый вывод CI

После создания PR в GitHub будет видно:

```
✅ Lint - passed (XXs)
✅ TypeCheck - passed (XXs)
✅ Build - passed (XXs)
```

## Особенности

- ✅ Параллельное выполнение jobs (быстрая обратная связь)
- ✅ Кэширование зависимостей (быстрая установка)
- ✅ Использование официальных actions
- ✅ Поддержка pnpm workspaces через turbo

## ✅ DoD выполнено

- [x] GitHub Actions workflow создан
- [x] Jobs для lint, typecheck, build настроены
- [x] Триггеры на push и pull_request настроены
- [x] Используется правильная версия pnpm и Node.js
- [x] Кэширование настроено
- [x] README обновлен с информацией о CI

Готово к тестированию! 🎉

**Для проверки:** Создайте PR в GitHub и проверьте что CI проходит успешно.
