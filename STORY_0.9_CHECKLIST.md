# Story 0.9 — CI pipeline минимальный

## ✅ Выполнено

### GitHub Actions
- [x] `.github/workflows/ci.yml` - CI pipeline создан
- [x] Триггеры на push и pull_request
- [x] Jobs для lint, typecheck, build

### Конфигурация
- [x] Использует pnpm 8.15.0
- [x] Node.js 18
- [x] Кэширование pnpm
- [x] Параллельное выполнение jobs

## 📋 DoD Checklist

### DoD: PR зелёный

**Для проверки выполните:**

1. Создать PR в GitHub
2. Проверить что CI запустился автоматически
3. Убедиться что все jobs прошли успешно

**Ожидаемое поведение:**
- ✅ Lint job проходит
- ✅ TypeCheck job проходит
- ✅ Build job проходит
- ✅ PR показывает зелёный статус

## 🧪 Ручной тест

### Тест 1: Локальная проверка команд

```bash
# Lint
pnpm lint

# TypeCheck
pnpm typecheck

# Build
pnpm build
```

Все команды должны выполниться без ошибок.

### Тест 2: Создать PR

1. Создать новую ветку
2. Внести изменения
3. Создать PR
4. Проверить что CI запустился

**Ожидаемое поведение:**
- CI запускается автоматически
- Все jobs выполняются
- PR показывает зелёный статус

## 📁 Структура

```
.github/
└── workflows/
    └── ci.yml              ✅ CI pipeline
```

## 🔧 Конфигурация

### Триггеры

- `push` на ветки: `main`, `develop`
- `pull_request` на ветки: `main`, `develop`

### Jobs

1. **lint** - Проверка кода линтером
2. **typecheck** - Проверка типов TypeScript
3. **build** - Сборка всех приложений

### Параллельное выполнение

Все jobs выполняются параллельно для быстрой обратной связи.

## 📝 Артефакты

### Вывод CI

После создания PR ожидается:

```
✅ Lint - passed
✅ TypeCheck - passed  
✅ Build - passed
```

### Скриншот CI

После создания PR в GitHub будет видно:
- Статус каждого job
- Время выполнения
- Логи выполнения

## ✅ Готово к тестированию

Все файлы созданы. Для проверки DoD создайте PR в GitHub.

**Важно:** Убедитесь что репозиторий подключен к GitHub и Actions включены.
