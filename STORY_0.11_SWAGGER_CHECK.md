# Story 0.11 — Проверка Swagger (A и B)

## Проверка A: Global Prefix

✅ **Нет global prefix**
- В `main.ts` нет `app.setGlobalPrefix()`
- Swagger должен быть по `/docs` (не `/api/docs`)

## Проверка B: Правильный URL

✅ **Правильный URL**
- Сервер слушает на порту 3001 (из лога: `port: 3001`)
- Проверяю `http://localhost:3001/docs` - правильно

## Проблема

После `[DEBUG] Setting up Swagger at /docs...` нет сообщений:
- `[DEBUG] SwaggerModule.setup() completed successfully` - не появляется
- `📚 Swagger documentation available` - не появляется
- `🚀 API server is running` - не появляется

Это значит, что выполнение останавливается на `SwaggerModule.setup()`.

## Решение

Для Swagger с Fastify требуется `@fastify/static` для раздачи статических файлов.

### Установка

```bash
cd apps/api && npm install @fastify/static
```

### Результат

После установки `@fastify/static`:
- ✅ Swagger setup должен завершиться успешно
- ✅ `/docs` должен быть доступен
- ✅ `/docs-json` должен быть доступен

## Текущий статус

- ✅ Проверка A: нет global prefix
- ✅ Проверка B: правильный URL
- ⚠️ Swagger setup не завершается (требуется `@fastify/static`)
