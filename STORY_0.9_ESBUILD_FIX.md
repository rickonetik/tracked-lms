# Story 0.9 — Исправление ошибки esbuild platform ✅

## Проблема

Build job падает с ошибкой:
```
You installed esbuild for another platform than the one you're currently using.
Specifically the "@esbuild/darwin-arm64" package is present but this platform
needs the "@esbuild/linux-x64" package instead.
```

Причина: В репозитории или кэше есть `node_modules` или `.vite` с неправильной платформой (darwin-arm64 вместо linux-x64). Это происходит когда зависимости устанавливаются на macOS и кэш попадает в git или CI.

## Решение

✅ Добавлен шаг "Clean build artifacts" перед установкой зависимостей:
- Удаляются все `node_modules` (корень, apps, packages)
- Удаляются кэши сборки (`.vite`, `dist`, `build`)

Это гарантирует, что все зависимости будут установлены заново для правильной платформы (linux-x64) в CI.

## Следующие шаги

1. **Обновить страницу PR** (Ctrl+F5)
2. **Дождаться завершения нового CI run** (2-3 минуты)
3. **Проверить статус Build job:**
   - ✅ Должен пройти успешно

**Исправление применено!** 🎉

Теперь все зависимости будут устанавливаться заново для правильной платформы в CI.
